"""
LiveStream TTS — Shared Common Library

Provides cross-cutting utilities used by all microservices:
- Error codes & exception hierarchy
- Configuration loading (env vars + ConfigMap)
- gRPC middleware (logging, tracing, retry)
- Structured logging
- Health check helpers
"""

__version__ = "0.1.0"
