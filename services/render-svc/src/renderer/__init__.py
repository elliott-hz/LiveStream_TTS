"""
renderer package — Visual rendering pipeline components.

Modules:
  lipsync     — Mock Wav2Lip: generate blendshape weights from audio
  compositor  — Mock FFmpeg: compose video frame from avatar + overlays
  overlays    — Product cards, price tags, coupons, watermarks
"""
