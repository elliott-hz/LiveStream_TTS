# ── Core ──
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# ── gRPC ──
grpcio>=1.60.0
grpcio-tools>=1.60.0
grpcio-health-checking>=1.60.0
grpcio-reflection>=1.60.0
protobuf>=4.25.0

# ── Observability ──
structlog>=24.1.0

# ── HTTP ──
httpx>=0.26.0

# ── Infra ──
prometheus-client>=0.19.0

# ── Shared libs (editable installs from monorepo) ──
../../libs/common

# ── Generated proto stubs ──
../../libs/proto

# ── Dev / Test ──
pytest>=8.0.0
pytest-asyncio>=0.23.0
