apiVersion: v1
kind: Service
metadata:
  name: billing-svc
  namespace: livestream-tts
  labels:
    app: billing-svc
spec:
  selector:
    app: billing-svc
  ports:
    - name: http
      port: 8015
      targetPort: http
    - name: grpc
      port: 50051
      targetPort: grpc
  type: ClusterIP
