apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-svc
  namespace: livestream-tts
  labels:
    app: billing-svc
    service-type: billing
spec:
  replicas: 2
  selector:
    matchLabels:
      app: billing-svc
  template:
    metadata:
      labels:
        app: billing-svc
    spec:
      containers:
        - name: billing-svc
          image: livestream-tts/billing-svc:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8015
              name: http
            - containerPort: 50051
              name: grpc
          envFrom:
            - configMapRef:
                name: billing-config
          env:
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: host
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: password
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
