apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-svc
  namespace: livestream-tts
  labels:
    app: product-svc
    service: product
    tier: backend
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: product-svc
  template:
    metadata:
      labels:
        app: product-svc
        service: product
        tier: backend
    spec:
      serviceAccountName: product-svc
      terminationGracePeriodSeconds: 30
      containers:
        - name: product-svc
          image: ghcr.io/livestream-tts/product-svc:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 50051
              name: grpc
              protocol: TCP
            - containerPort: 8003
              name: http
              protocol: TCP
          env:
            - name: ENV
              value: "production"
            - name: PRODUCT_SVC_GRPC_PORT
              value: "50051"
            - name: PRODUCT_SVC_HTTP_PORT
              value: "8003"
            - name: PRODUCT_SVC_DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: product-svc-config
                  key: DB_HOST
            - name: PRODUCT_SVC_DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: product-svc-config
                  key: DB_PORT
            - name: PRODUCT_SVC_DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: product-svc-config
                  key: DB_NAME
            - name: PRODUCT_SVC_DB_USER
              valueFrom:
                secretKeyRef:
                  name: product-svc-secret
                  key: DB_USER
            - name: PRODUCT_SVC_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: product-svc-secret
                  key: DB_PASSWORD
            - name: PRODUCT_SVC_DB_POOL_SIZE
              value: "10"
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 8003
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 8003
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /etc/config/product-svc
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: product-svc-config
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: product-svc
  namespace: livestream-tts
  labels:
    app: product-svc
