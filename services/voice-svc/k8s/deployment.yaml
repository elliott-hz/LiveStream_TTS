apiVersion: apps/v1
kind: Deployment
metadata:
  name: voice-svc
  namespace: livestream-tts
  labels:
    app: voice-svc
    service: voice
    tier: backend
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: voice-svc
  template:
    metadata:
      labels:
        app: voice-svc
        service: voice
        tier: backend
    spec:
      serviceAccountName: voice-svc
      terminationGracePeriodSeconds: 30
      containers:
        - name: voice-svc
          image: ghcr.io/livestream-tts/voice-svc:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 50051
              name: grpc
              protocol: TCP
            - containerPort: 8007
              name: http
              protocol: TCP
          env:
            - name: ENV
              value: "production"
            - name: VOICE_SVC_GRPC_PORT
              value: "50051"
            - name: VOICE_SVC_HTTP_PORT
              value: "8007"
            - name: VOICE_SVC_DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: voice-svc-config
                  key: DB_HOST
            - name: VOICE_SVC_DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: voice-svc-config
                  key: DB_PORT
            - name: VOICE_SVC_DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: voice-svc-config
                  key: DB_NAME
            - name: VOICE_SVC_DB_USER
              valueFrom:
                secretKeyRef:
                  name: voice-svc-secret
                  key: DB_USER
            - name: VOICE_SVC_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: voice-svc-secret
                  key: DB_PASSWORD
            - name: VOICE_SVC_DB_POOL_SIZE
              value: "10"
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 8007
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 8007
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /etc/config/voice-svc
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: voice-svc-config
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: voice-svc
  namespace: livestream-tts
  labels:
    app: voice-svc
