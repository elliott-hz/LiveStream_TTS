apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-sync-svc
  namespace: livestream-tts
  labels:
    app: platform-sync-svc
    service-type: platform-sync
spec:
  replicas: 2
  selector:
    matchLabels:
      app: platform-sync-svc
  template:
    metadata:
      labels:
        app: platform-sync-svc
    spec:
      containers:
        - name: platform-sync-svc
          image: livestream-tts/platform-sync-svc:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8013
              name: http
            - containerPort: 50051
              name: grpc
          envFrom:
            - configMapRef:
                name: platform-sync-config
          env:
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: host
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: password
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
